$("#follow_button").html("<%= j(render(partial: 'users/follow_button', locals: { user: @user })) %>");

$("#followers").html('<%= @user.followers.count %>');
$("#followings").html('<%= current_user.followings.count %>');
