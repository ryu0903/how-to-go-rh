<% provide(:title, 'Login for recruiter') %>
<div class="row">
    <p></p>
    <div class="col-md-6 col-md-offset-3">
        <%= form_with(url: login_path, scope: :session, local: true) do |f| %>
            <p></p>
            <div class="form-group">
                <%= f.label :email, "Email" %>
                <%= f.email_field :email, class: "form-control", value: 'yamada@example.com' %>
            </div>
            
            <div class="form-group">
                <%= f.label :password, "Password" %>
                <%= f.password_field :password, class: "form-control", value: 'password' %>
            </div>
            
            <div class="form-group">
                <%= f.label :remember_me do %>
                <%= f.check_box :remember_me %>
                <sapn>Keep Login</sapn>
                <% end %>
            </div>
            
            <%= f.submit "Login", class: "btn btn-primary btn-block" %>
            
            <br><p>上記の「Login」ボタンを押して頂ければ、管理ユーザーとしてログインが可能でございます。</p>
            
        <% end %>
    </div>
</div>
