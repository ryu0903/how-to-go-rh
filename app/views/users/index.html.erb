<% provide(:title, 'All Users') %>
<div class="container">
  <h1>All Users</h1>
  <%= paginate @users %>  
    <ul class="users list-unstyled">
      <%= render 'users', users: @users %>
    </ul>
  <%= paginate @users %>  
</div>
