<%= form_with(model: current_user.follow_relationships.find_by(follow_id: user.id), method: :delete ) do |f| %>
  <%= hidden_field_tag :follow_id, user.id %>
  <%= f.submit 'Following', class: "btn btn-danger btn-block" %>
<% end %>